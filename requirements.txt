# Runtime dependencies for the Dubai real-estate Telegram channel pipeline.
# Python 3.11+
requests==2.32.3
PyYAML==6.0.2
feedparser==6.0.11
python-dateutil==2.9.0.post0
Pillow==10.4.0
anthropic==1.7.0
# Reading private Telegram channels through a user session (see docs/SETUP.md).
telethon==1.45.0

# Text of the PDFs the owner forwards to the bot (pipeline/inbox.py).
pypdf==5.1.0
# Whisper transcription of the owner's videos (needs OPENAI_API_KEY) and the
# optional OpenAI provider (LLM_PROVIDER=openai).
openai==3.19.0

# Dev / CI
pytest==8.3.3
